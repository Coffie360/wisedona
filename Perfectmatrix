Hello world

function levels(group){
array=["A"];
   var dl = array.lenght*group +1;
   var dl1 = dl + 1;
   console.log(dl,dl1);
   }
   levels(1);
   
const Parallelism = 2; 
const asyncBatch = require('async-batch').default;
const arr = [1, 2, 3];
const asyncMethod = async (val) => { console.log(`Value: ${val}`); };

await asyncBatch(arr, asyncMethod, Parallelism);
